services: mongodb
language: ruby
before_install:
  - gem install bundler -v '1.5.1'
  - bundle -v
before_script:
  - psql -c 'create database schnitzel_test;' -U postgres
rvm:
  - 2.1.0
script: bundle exec rspec spec
